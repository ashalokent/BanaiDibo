{
  "name": "banaidibo",
  "version": "1.0.0",
  "description": "BanaiDibo – Local Services Platform",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
